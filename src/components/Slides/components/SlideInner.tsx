import { styled } from '@mui/material/styles';

const SlideInner = styled('div')({
  position: 'relative',
  overflow: 'hidden',
  height: 'auto', // Height slide here
});

export default SlideInner;
